// Fixed: Boolean without quotes
function main() {
    var isActive = true;  // Boolean, not string
    var count = 0
    
    if ( isActive ) {
        println( "System is active" )
    }
    
    if ( count ) {
        println( "Count is truthy" )
    } else {
        println( "Count is falsey" )
    }
}

main()
