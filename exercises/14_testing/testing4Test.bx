/**
 * TODO: Write tests for sumArray()
 */
class extends="testbox.system.BaseSpec" {
    function run() {
        describe( "Testing 4 - sumArray()", () => {
            it( "TODO: replace this with real expectations", () => {
                include "testing4.bxs"
                // TODO: Write real expectations for sumArray()
                expect( true ).toBeFalse()
            })
        })
    }
}
