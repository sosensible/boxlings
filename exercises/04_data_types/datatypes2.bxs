// TODO: Fix the calculation to get integer division

function main() {
    var a = 10;
    var b = 3;
    
    var remainder = a % b;
    var quotient = a / b;  // This gives decimal, we need integer!
    
    println( "Quotient: #quotient#" );
    println( "Remainder: #remainder#" );
}

main();
