// TODO: Add proper type checks using BoxLang type-checking functions

function processValue( value ) {
    // TODO: Check what type we received and handle accordingly
    // Use isArray(), isStruct(), isNumeric(), isBoolean(), etc.
    
    println( "Processing: #value.toString()#" );
}

function main() {
    processValue( [1,2,3] );
    processValue( {name: "test"} );
    processValue( 42 );
    processValue( "hello" );
}

main();
