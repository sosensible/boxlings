// TODO: Fix the string concatenation!
// In BoxLang, use & (ampersand) to concatenate strings, not +

function main() {
    var firstName = "Box";
    var lastName = "Lang";
    var fullName = firstName + lastName;  // Wrong operator!
    
    println( "Full name: #fullName#" );
}

main();
