// TODO: Fix the array access!
// Remember: Arrays in BoxLang start at 1, not 0!

function main() {
    var colors = [ "red", "green", "blue" ];
    
    // This is wrong - BoxLang arrays are 1-based!
    println( "First color: #colors[0]#" );
    println( "Second color: #colors[1]#" );
}

main();
