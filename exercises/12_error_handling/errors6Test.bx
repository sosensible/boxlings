/**
 * TestBox Test for errors6
 */
class extends="testbox.system.BaseSpec" {
    function run() {
        describe( "Error Handling 6 - Safe Divide", () => {
            it( "returns 0 on divide by zero", () => {
                include "errors6.bxs"
                expect( variables.result ).toBe( 0 )
            })
        })
    }
}
