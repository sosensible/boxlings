/**
 * TestBox Test for functional6
 */
class extends="testbox.system.BaseSpec" {
    function run() {
        describe( "Functional 6 - chain", () => {
            it( "filters and doubles", () => {
                include "functional6.bxs"
                expect( variables.result ).toBe( [ 4, 8 ] )
            })
        })
    }
}
