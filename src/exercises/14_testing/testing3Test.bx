/**
 * TODO: Write tests for buildUser()
 */
class extends="testbox.system.BaseSpec" {
    function run() {
        describe( "Testing 3 - buildUser()", () => {
            it( "TODO: replace this with real expectations", () => {
                include "testing3.bxs"
                // TODO: Write real expectations for buildUser()
                expect( true ).toBeFalse()
            })
        })
    }
}
