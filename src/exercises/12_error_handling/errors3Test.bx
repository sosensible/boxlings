/**
 * TestBox Test for errors3
 */
class extends="testbox.system.BaseSpec" {
    function run() {
        describe( "Error Handling 3 - Finally", () => {
            it( "runs cleanup", () => {
                include "errors3.bxs"
                expect( variables.result ).toBeTrue()
            })
        })
    }
}
