class extends="testbox.system.BaseSpec" {
    function run() {
        describe( "Structs Exercise 4 - Nested Structs", () => {
            it( "should access nested properties", () => {
                include "structs4.bxs"
                expect( main() ).toInclude( "Houston" )
            })
        })
    }
}
