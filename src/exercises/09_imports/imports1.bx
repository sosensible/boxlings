// TODO: Import the User class
// There is a User class in the `src` directory.  How do you import it?

class {
    function main( args = [] ) {
        // TODO: Add import statement at the top
        var luis = new User( "Luis", 40 )
        println( luis.getName() )
    }
}
