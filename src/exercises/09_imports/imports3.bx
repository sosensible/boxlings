// TODO: Use an import alias
// Sometimes you need to import classes with the same name

import java:java.util.Date as JavaDate

class {
    function main( args = [] ) {
        // TODO: Fix the instantiation - use the alias!
        var jDate = new Date()

        println( "Java Date: #jDate.toString()#" )
    }
}
