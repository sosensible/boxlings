// TODO: Create and use imported classes

import java:java.util.HashMap

class {
    function main( args = [] ) {
        // TODO: Create a HashMap instance
        var map = ???

        map.put( "language", "BoxLang" )
        map.put( "version", "1.0" )

        return "Language: #map.get( 'language' )#"
    }
}
