/**
 * TestBox Test for variables3 exercise
 *
 * READ THIS FIRST! This test shows you what your code should do.
 *
 * Your task: Make the exercise code pass these tests!
 */
class extends="testbox.system.BaseSpec" {

    function run() {
        describe( "Variables Exercise 5", () => {

            it( "should have the correct convention casing for constants", () => {
                include "variables5.bxs"

                expect( variables ).toHaveKeyWithCase( "MAX_POINTS" )
            })

        })
    }
}
