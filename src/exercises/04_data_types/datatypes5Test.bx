class extends="testbox.system.BaseSpec" {
    function run() {
        describe( "Data Types Exercise 5 - Arrays", () => {
            it( "should create array with correct syntax", () => {
                include "datatypes5.bxs"
                expect( isArray( variables.fruits ) ).toBeTrue()
                expect( variables.fruits.len() ).toBe( 3 )
                expect( variables.fruits[1] ).toBe( "apple" )
            })
        })
    }
}
