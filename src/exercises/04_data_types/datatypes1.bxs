// TODO: Fix the string concatenation!
// In BoxLang, use & (ampersand) to concatenate strings, not +

function main() {
    var firstName = "Box"
    var lastName = "Lang"
    variables.fullName = firstName + lastName;  // Wrong operator!

    println( "Full name: #variables.fullName#" )
}

main()
