class extends="testbox.system.BaseSpec" {
    function run() {
        describe( "Data Types Exercise 3 - Booleans", () => {
            it( "should produce correct output", () => {
                include "datatypes3.bxs"
                expect( main() ).toInclude( "Count is truthy" )
            })
        })
    }
}
