class extends="testbox.system.BaseSpec" {
    function run() {
        describe( "Strings Exercise 1 - Interpolation", () => {
            it( "interpolates inside strings using hash signs", () => {
                include "strings1.bxs"
                expect( main() ).toInclude( "Welcome to BoxLang" )
            })
        })
    }
}
