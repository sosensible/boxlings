var person = { name: "Alice", age: 30, city: "Boston" }

var hasAge   = person.keyExists( "age" )
var keys     = person.keyArray()
var vals     = person.valueArray()
var smaller  = person.delete( "city" )
var keyCount = person.count()
